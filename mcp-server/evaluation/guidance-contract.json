{
  "revision": "2026-09-17-lean-pinned-v4",
  "files": 103,
  "basis": "2026-09-17 reviewed MCP economy refactor plus safe duplicate-hand collapsing, short action aliases, lean TOC/context projection, and required fixed-action parameter internalization: public tools keep session/state, delta hashes, native action IDs, combat decision tokens, fixed map_id/revision, and event reading_id server-side; JSON payloads are single-copy and auto-TOON when losslessly smaller; primitive arrays page directly as items; normal transport/success/mechanics audit metadata and narrative observer bookkeeping are omitted; hand TOC carries current cost; multi-card reads share an inline budget; default combat hand collapses only cards whose complete observed state differs solely by UUID/hand-index/render provenance, preserving positional facts such as Hermit Dead On; exact physical hand refs remain available through context. Only required const/single-enum parameters are stripped from public schemas/examples and restored from the exact pinned native action before dispatch, preserving optional-parameter semantics. No live-game acceptance claim."
}
